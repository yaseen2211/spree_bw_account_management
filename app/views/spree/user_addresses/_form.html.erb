<%= form_with(model: spree_user_address, local: true) do |form| %>
  <% if spree_user_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spree_user_address.errors.count, "error") %> prohibited this spree_user_address from being saved:</h2>

      <ul>
      <% spree_user_address.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
